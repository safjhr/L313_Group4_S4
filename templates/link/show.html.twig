{% extends 'base.html.twig' %}

{% block title %}Link{% endblock %}

{% block body %}
    <h1>Link</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ link.id }}</td>
            </tr>
            <tr>
                <th>Lien_url</th>
                <td>{{ link.lienUrl }}</td>
            </tr>
            <tr>
                <th>Lien_titre</th>
                <td>{{ link.lienTitre }}</td>
            </tr>
            <tr>
                <th>Lien_desc</th>
                <td>{{ link.lienDesc }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_link_index') }}">back to list</a>

    <a href="{{ path('app_link_edit', {'id': link.id}) }}">edit</a>

    {{ include('link/_delete_form.html.twig') }}
{% endblock %}
