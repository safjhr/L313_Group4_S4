{% extends 'base.html.twig' %}

{% block title %}Modifier le mot clé{% endblock %}

{% block body %}
    <h1>Modifier le mot clé</h1>

    {{ include('keyword/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('app_keyword_index') }}" class="btn btn-secondary">Retour à la liste</a>

    {{ include('keyword/_delete_form.html.twig') }}
{% endblock %}
